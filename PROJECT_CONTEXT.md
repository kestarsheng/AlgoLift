# 算法学习工作台 · 需求与现状分析文档

> 分析对象：已发布公开页 `https://workbuddy.link/p/COi0ZRCtYSQoF97es81CYk`
> 数据源：单张资料库数据表（`DATABASE_ID = HGI8j4vGy0WQ9fBXcTwe58`），通过 WorkBuddy 智能页 SDK（`window.__SMART_PAGE__.database`）读写。
> 架构本质：**单库 + `模块`(select) 字段区分 7 类记录**；前端单文件 HTML，无自有后端、无账号体系。

---

## 1. 核心实体与数据结构

| # | 实体（记录类型） | 关键字段 | 备注 |
|---|---|---|---|
| E1 | **分类 Category** | `标题`(名称) | 一道题可归属多个分类（多对多） |
| E2 | **题目 Problem** | `标题`、`难度`(简单/中等/困难)、`分类`(存分类 `_id` 的 JSON 数组)、`内部笔记`(富文本 HTML)、`关联笔记`(存笔记 `_id` 的 JSON 数组) | 刷题练习的核心对象 |
| E3 | **练习记录 PracticeRecord** | `题目ref`(题目**标题文本**)、`日期`、`一遍做对`(布尔)、`备注` | 同一题可记多次（一遍/二遍/…） |
| E4 | **错题 Wrong** | `标题`、`分类`(单值自由文本"大类")、`难度`、`错因复盘`(富文本)、`题解链接`(JSON 数组 `{名称,链接}`)、`关联笔记`(JSON 数组) | 注意：错题的"分类"与 E1 分类表**不是同一套** |
| E5 | **题解笔记 Note** | `标题`、`内容`(富文本)、`题解链接`(JSON 数组)、`关联题目`(JSON 数组，可指向题目或错题的 `_id`) | 飞书式左栏列表 + 正文 |
| E6 | **待办 Todo** | `标题`、`日期`、`优先级`(P0/P1/P2)、`状态`(待办/进行中/已完成)、`备注` | 独立，无关联 |
| E7 | **进度 Progress** | `标题`(主题/模块)、`日期`、`进度`(0–100 数值)、`描述` | 独立进度条记录 |

### 实体间关系

```
           ┌─────────────┐
           │  E1 分类     │
           └──────┬──────┘
      多对多(M:N) │ 题目.分类 = [分类_id...]  (E2.分类 存的是分类记录的主键数组)
                  │
           ┌──────▼──────┐
           │  E2 题目     │
           └──┬──────┬───┘
   1对多(1:N) │      │ 多对多(M:N)
              │      │ 题目.关联笔记 ↔ 笔记.关联题目 (双向)
   ┌──────────▼───┐  │
   │ E3 练习记录   │  │
   │ (靠 题目ref=  │  │
   │  题目.标题 文本│  │
   │  关联，非_id) │  │
   └──────────────┘  │
                     │
           ┌─────────▼───┐   多对多(M:N)
           │ E4 错题      │── 错题.关联笔记 ↔ 笔记.关联题目
           │ (分类=自由文本│
           │  非 E1 表)   │
           └─────────────┘

   E5 题解笔记 ── 多对多 ──► E2 题目 / E4 错题
   E6 待办、E7 进度：独立实体，无任何关联
```

**关键设计点（影响后续重构）：**
- **两套"分类"并存**：题目的分类走规范化 `分类` 表（E1）+ 多对多；错题的分类只是自由文本字段，二者互不相通。
- **练习记录靠"标题文本"关联题目**（`题目ref === 题目.标题`），而非主键 `_id`——改题目标题会令历史练习记录失去归属。
- **多对多全靠 JSON 数组塞进 `text` 字段**（存对方 `_id`），没有中间表，也没有外键约束。
- **富文本直接存原始 HTML**（读取时做基础 sanitize，去掉 `<script>`/`on*` 属性）。

---

## 2. 用户核心操作流程

**进入页面**
1. 打开公开链接 → 顶部 Header（同步状态、目标日、导出/导入）。
2. 顶部 6 个 Tab：刷题练习（默认）/ 错题本 / 题解笔记 / 待办 / 学习进度 / 数据概览。

**刷题练习（主流程）**
3. 看板首页：按分类展示卡片（每类题量），可「+ 新增题目」「+ 新建分类」「管理分类」。
4. 点分类 → 进入题列表（按标题排序，显示练习次数 / 关联笔记数 / 最近日期）。
5. 点题目 → 题目详情：内部笔记、练习记录列表、关联题解笔记；可操作：记一次练习、编辑内部笔记、关联笔记、管理分类、编辑、删除。
6. 记练习 → 弹窗填日期 / 是否一遍做对 / 备注 → 写入。

**错题本**
7. 看板按"大类" → 列表 → 详情：编辑错因复盘（富文本）、增删题解链接、关联笔记、编辑、删除。

**题解笔记**
8. 左栏笔记列表 + 右栏正文；可改名、编辑内容、加题解链接、关联题目/错题、删除。

**待办 / 学习进度**
9. 待办：列表（逾期标红、按状态/日期排序）+ 增改删；进度：百分比卡片列表 + 增改删。

**数据概览**
10. 统计卡（题数、练习次数、一遍过正确率、错题数、笔记数、待办完成率）+ 难度分布柱状图 + 刷题打卡热力图（悬浮显示当天次数）+ 目标倒计时。

**通用动作**
- 任意模块均有增/改/删弹窗；删除有二次确认。
- 顶部「导出备份」(JSON) /「导入恢复」(JSON)。

---

## 3. 现有的数据与状态管理

### 3.1 写死（硬编码）的内容
- **选项常量**：难度（简单/中等/困难）、优先级（P0/P1/P2）、状态（待办/进行中/已完成）。
- **分类预设词表**：`['数组','链表','哈希表','字符串','栈','队列','树','二叉树','堆','图','动态规划','贪心','二分查找','回溯','数学','位运算','并查集','设计','其他']`（仅在错题分类输入框的 datalist 里提示用）。
- **展示参数**：热力图天数窗口（119 天）、配色（暖橙主题）、卡片阈值（练习≥1/3/5/8 映射到 4 级颜色）。
- **按钮/标签文案**、模块中文名。

### 3.2 用户动态产生的数据
- 全部 7 类业务记录（E1–E7），均为用户在使用中创建/编辑/删除。

### 3.3 数据存放位置
| 数据 | 位置 | 性质 |
|---|---|---|
| 业务记录 E1–E7 | **WorkBuddy 资料库云端表**（`HGI8...`） | 唯一真实数据源，多人/多设备共享同一份 |
| 全量记录的内存副本 | 前端 `DB` 数组（启动时 `db.query` 加载） | 运行时缓存，刷新即重拉 |
| 表单草稿 | `localStorage`（key 形如 `wb_algo_form_xxx`） | 输入中途临时缓存，每 800ms 落盘、保存后清除 |
| 目标日 | `localStorage`（`wb_algo_target`） | 纯前端偏好，不上云 |
| 图片 | 经 `db.uploadImage` 上传到资料库，URL 内嵌进富文本 | 云存储；本地 IndexedDB 图片代码**已定义但未被调用（死代码）** |

> **结论**：当前没有"本地优先"存储——`localStorage` 只是草稿/偏好，主力是资料库云端。所有打开链接的人读写的是**同一个共享库**，不存在"我的数据"。

---

## 4. 页面模块与功能点

### Header（全局）
- 同步状态指示器（已同步 / 同步中 / 离线模式）
- 目标日选择器（写入 localStorage）
- 按钮：导出备份、导入恢复（JSON）
- 热力图悬浮提示气泡（全局 mousemove 委托）

### Tab 1 · 刷题练习
- **看板首页**：分类卡片（题量+名称）、新增题目、新建分类、管理分类（编辑/删除分类）
- **分类列表页**：该分类下题目项（练习次数/笔记数/最近日期）、分类 chip
- **题目详情**：内部笔记区、练习记录列表（第N次/日期/一遍过标记/删除）、关联题解笔记 chip、按钮（记一次练习 / 编辑内部笔记 / 关联笔记 / 管理分类 / 编辑 / 删除）

### Tab 2 · 错题本
- **看板**：按"大类"聚合卡片
- **列表**：错题项（题解链接数、关联笔记数）
- **详情**：错因复盘（富文本）、题解链接（可多、可增删）、关联笔记、按钮（编辑错因 / +题解链接 / 关联笔记 / 编辑 / 删除）

### Tab 3 · 题解笔记
- **左栏**：笔记列表（点击切换）
- **右栏详情**：标题（可改名）、内容（富文本）、题解链接（可多）、关联题目/错题 chip；按钮（编辑内容 / +链接 / 关联题目 / 删除）

### Tab 4 · 待办
- 列表（逾期标红、P0/P1/P2 徽章、状态徽章），点击进编辑
- 新增/编辑表单：标题、日期、优先级、状态、备注

### Tab 5 · 学习进度
- 进度卡片列表（百分比进度条 + 描述）
- 新增/编辑表单：主题、日期、完成度(0–100)、描述

### Tab 6 · 数据概览
- 统计卡：题目数 / 练习次数 / 一遍过正确率 / 错题数 / 笔记数 / 待办完成率
- 难度分布柱状图（内联 SVG/CSS）
- 刷题打卡热力图（近 119 天，颜色深浅=当天次数，悬浮提示）
- 目标倒计时（基于 localStorage 目标日）

### 通用交互组件
- 模态弹窗（所有增改删）、二次确认删除、Toast 提示、富文本编辑器（加粗/代码块/插图）

---

## 5. 缺失的生产级能力（要变成"所有人可用的多用户应用"必须补的）

### 5.1 身份与隔离（最致命）
- ❌ **无用户账号 / 注册 / 登录**：任何人拿链接即共享同一库，数据全公开。
- ❌ **无多租户隔离**：没有 `user_id`，无法做到"每人独享自己的数据"。
- ❌ **无鉴权 / 授权 / 会话**：无 JWT、无 Cookie/Session、无密码哈希（bcrypt）。
- ❌ **无权限模型**：无法区分"自己的"与"别人的"数据。

### 5.2 后端与持久化
- ❌ **无自有后端 / API**：前端直连 WorkBuddy SDK，没有可掌控的服务器。
- ❌ **无真正的数据库**：依赖资料库单表，无结构化 RDBMS、无索引、无外键。
- ❌ **无服务端校验**：所有字段校验在前端，篡改 JS 即可写入非法数据。
- ❌ **关联靠文本而非主键**（练习记录用标题关联题目），无引用完整性，删除主对象会留下悬空引用。
- ❌ **无中间表**：多对多用 JSON 数组模拟，无法高效查询"某笔记被哪些题引用"。

### 5.3 规模与性能
- ❌ **全量加载到前端**：启动拉取整张表（分页上限约 2 万行），数据量大时卡顿、超量截断。
- ❌ **无服务端检索/分页/过滤**：所有统计（热力图、正确率）在浏览器现算。
- ❌ **无并发控制 / 乐观锁**：多人同时编辑同条记录会互相覆盖（最后写入胜出）。

### 5.4 安全与合规
- ❌ **无 CORS / 速率限制 / 防滥用**：公开页无任何请求防护。
- ❌ **富文本 XSS 风险**：sanitize 仅是基础白名单，非完整 HTML 净化（如 CSS 注入、钓鱼链接未拦截）。
- ❌ **无图片病毒扫描 / 大小策略**：仅前端 10MB 限制。
- ❌ **无审计日志 / 操作留痕**。

### 5.5 工程化
- ❌ **无数据模型版本迁移**（Alembic 之类）。
- ❌ **无自动化测试**（鉴权、隔离逻辑无覆盖）。
- ❌ **无离线优先 / 冲突解决策略**（仅"重拉=覆盖"）。
- ❌ **无国际化 / 无障碍**（基础 a11y 缺失）。
- ❌ **部署绑定 WorkBuddy 托管**，不便迁移到自有域名/基础设施。
- ❌ **隐私合规缺口**：公开链接暴露全部学习者数据，不符合"个人数据最小化"原则。

### 5.6 由现状直接暴露的产品债
- 两套"分类"体系并存，需统一为一个规范化的标签/分类模型。
- 练习记录应改存题目 `_id`（而非标题文本），并建立真正的外键关系。
- 多对多应下沉为关系表（如 `题目_笔记`、`笔记_题目` 关联表），而非 JSON-in-text。
- 单表 + `模块` 判别字段应拆分为按实体分表/分模型，提升类型安全与查询效率。
