# Electron 桌面端可选配置，优先级高于 backend/.env。
# 通常留空即可——主进程会自动读取 backend/.env 中的 DATABASE_URL 和 JWT_SECRET。
# 仅当需要在 Electron 层覆盖后端配置时才填写：
# DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"
# JWT_SECRET="replace-with-a-long-random-secret"