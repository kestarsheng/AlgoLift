import { describe, expect, it } from 'vitest'; import { router } from '../src/router';
describe('todo route', () => { it('is lazy loaded and titled', () => { const route = router.getRoutes().find((item) => item.path === '/todos'); expect(route?.meta.title).toBe('待办'); expect(typeof route?.components?.default).toBe('function'); }); it('registers progress route', () => { const route = router.getRoutes().find((item) => item.path === '/progress'); expect(route?.meta.title).toBe('学习进度'); expect(typeof route?.components?.default).toBe('function'); }); });
