import { createPinia, setActivePinia } from 'pinia'; import { beforeEach, describe, expect, it, vi } from 'vitest'; import { api } from '../src/api'; import { useDashboardStore } from '../src/stores/dashboard';
vi.mock('../src/api', () => ({ api: { get: vi.fn() } }));
const page = { data: [], pagination: { page: 1, pageSize: 10, total: 0, totalPages: 0 } };
describe('dashboard store', () => { beforeEach(() => { setActivePinia(createPinia()); vi.clearAllMocks(); vi.mocked(api.get).mockResolvedValue({ data: page }); }); it('loads modules in parallel and aggregates counts', async () => { vi.mocked(api.get).mockImplementation((path) => Promise.resolve({ data: path === '/categories' ? { data: [{ id: 'c1', name: '数组', problemCount: 2 }] } : path === '/progresses' ? { data: [{ id: 'p1', title: '二分', progress: 60 }], pagination: page.pagination } : { data: [], pagination: { ...page.pagination, total: 3 } } })); const store = useDashboardStore(); await store.fetch(); expect(api.get).toHaveBeenCalledTimes(8); expect(store.categories.data).toHaveLength(1); expect(store.problems.data.pagination.total).toBe(3); expect(store.loading).toBe(false); }); it('keeps other modules when one request fails', async () => { vi.mocked(api.get).mockImplementation((path) => path === '/wrongs' ? Promise.reject(new Error('错题服务不可用')) : Promise.resolve({ data: path === '/categories' ? { data: [] } : page })); const store = useDashboardStore(); await store.fetch(); expect(store.wrongs.error).toContain('错题服务不可用'); expect(store.notes.error).toBe(''); expect(store.hasErrors).toBe(true); }); });
