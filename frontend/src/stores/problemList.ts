// Pinia store for filtered and paginated problem lists.
import { defineStore } from 'pinia';
import { api } from '../api';
import type { Difficulty, Pagination, ProblemListItem } from '../types';
interface ProblemResponse { data: ProblemListItem[]; pagination: Pagination }
export const useProblemListStore = defineStore('problemList', { state: () => ({ categoryId: '', items: [] as ProblemListItem[], pagination: { page: 1, pageSize: 10, total: 0, totalPages: 0 } as Pagination, keyword: '', difficulty: '' as Difficulty | '', loading: false, error: '' }), actions: { async fetch(categoryId?: string): Promise<void> { this.categoryId = categoryId ?? this.categoryId; this.loading = true; this.error = ''; try { const path = this.categoryId ? `/categories/${this.categoryId}/problems` : '/problems'; const response = await api.get<ProblemResponse>(path, { params: { page: this.pagination.page, pageSize: this.pagination.pageSize, keyword: this.keyword || undefined, difficulty: this.difficulty || undefined } }); this.items = response.data.data; this.pagination = response.data.pagination; } catch (error: unknown) { this.error = error instanceof Error ? error.message : '题目列表加载失败'; } finally { this.loading = false; } }, async search(): Promise<void> { this.pagination.page = 1; await this.fetch(); }, async setPage(page: number): Promise<void> { this.pagination.page = page; await this.fetch(); } } });
