# 后端运行环境变量示例 —— 复制为 .env 后按需修改

# 数据库连接串：本地 SQLite（默认）
# 换托管 Postgres（Neon / Supabase 免费层）时改为：
# DATABASE_URL=postgresql+psycopg://user:pass@host:5432/algolift
DATABASE_URL=sqlite:///./algolift.db

# JWT 签名密钥 —— 生产环境务必改成随机长字符串
JWT_SECRET=dev-secret-change-me
